# ContextBridge Configuration

APP_NAME=ContextBridge
DEBUG=true
HOST=0.0.0.0
PORT=8000

# Embedding model (sentence-transformers model name)
EMBEDDING_MODEL=all-MiniLM-L6-v2
EMBEDDING_DIMENSION=384

# Database
DB_PATH=./data/contextbridge.db

# Connector tokens (for future integrations)
GITHUB_TOKEN=
NOTION_TOKEN=
